---
interface Props {
  icon: string;
  title: string;
}
const { icon, title } = Astro.props;
---

<div class="value-card">
  <div class="value-card-icon" set:html={icon} />
  <h4>{title}</h4>
  <p><slot /></p>
</div>
